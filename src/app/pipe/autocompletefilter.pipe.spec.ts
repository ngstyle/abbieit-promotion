/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AutocompletefilterPipe } from './autocompletefilter.pipe';

describe('Pipe: Autocompletefiltere', () => {
  it('create an instance', () => {
    let pipe = new AutocompletefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
